<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:KinaUnaXamarin.ViewModels;assembly=KinaUnaXamarin"
             mc:Ignorable="d"
             x:Class="KinaUnaXamarin.Views.AccountPage"
             Title="Account">
    <ContentPage.BindingContext>
        <viewModels:AccountViewModel></viewModels:AccountViewModel>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout x:Name="LoginStackLayout" Orientation="Vertical" Margin="5">
            <StackLayout x:Name="OfflineStackLayout" IsVisible="False" BackgroundColor="Red">
                <Label Text="Offline: No internet connection." TextColor="White" FontSize="16" HorizontalOptions="Center"></Label>
            </StackLayout>
            <Image Source="KinaUnaLogo_1536x768.png" Aspect="AspectFit" VerticalOptions="Start" HorizontalOptions="Center" Margin="0, 0, 0, 20" ></Image>
            <Label x:Name="UsernameLabel" Text="{Binding Username, StringFormat='User Name: {0}'}" TextColor="Green" IsVisible="{Binding LoggedIn}" Margin="10"></Label>
            <Label x:Name="FullnameLabel" Text="{Binding FullName, StringFormat='Name: {0}'}" TextColor="Green" IsVisible="{Binding LoggedIn}" Margin="10"></Label>
            <Label x:Name="UserEmailLabel" Text="{Binding Email, StringFormat='Email: {0}'}" TextColor="Green" IsVisible="{Binding LoggedIn}" Margin="10"></Label>
            <Label x:Name="TimezoneLabel" Text="{Binding Timezone, StringFormat='Time Zone: {0}'}" TextColor="Green" IsVisible="{Binding LoggedIn}" Margin="10"></Label>
            <!--<Label x:Name="MessageLabel" Text="{Binding Message}" TextColor="Blue" Margin="10"></Label>-->
            <Button x:Name="LogInButton" Command="{Binding LoginCommand}" IsVisible="{Binding LoggedOut}" Text="Log in or Register" BackgroundColor="DarkGreen" TextColor="White" HeightRequest="60" FontSize="18" Margin="5"></Button>
            <Button x:Name="LogOutButton" Command="{Binding LogoutCommand}" IsVisible="{Binding LoggedIn}" Text="Log out" BackgroundColor="DarkOrchid" TextColor="White" HeightRequest="60" FontSize="18" Margin="5"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>